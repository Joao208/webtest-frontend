import styled from "styled-components";

export const Title = styled.p`
  font-family: "Montserrat", sans-serif;
  font-style: normal;
  font-weight: 500;
  font-size: 36px;
  line-height: 44px;

  margin: 0;

  color: #333333;
`;
